<Window x:Class="EarTrumpet.UI.Views.OnboardingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Theme="clr-namespace:EarTrumpet.UI.Themes"
        xmlns:gif="clr-namespace:XamlAnimatedGif;assembly=XamlAnimatedGif"
        xmlns:resx="clr-namespace:EarTrumpet.Properties"
        Title="BetterTrumpet"
        Width="520" Height="640"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Theme:Options.Source="App">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolVis"/>

        <!-- ═══ PALETTE ═══ -->
        <Color x:Key="AccentColor">#3B9EFF</Color>
        <Color x:Key="AccentHoverColor">#5BB0FF</Color>
        <Color x:Key="BgColor">#FF101014</Color>
        <Color x:Key="SurfaceColor">#FF18181E</Color>
        <Color x:Key="SurfaceHoverColor">#FF1F2028</Color>
        <Color x:Key="SurfaceSelectedColor">#FF1A2030</Color>
        <Color x:Key="StrokeColor">#12FFFFFF</Color>

        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
        <SolidColorBrush x:Key="StrokeBrush" Color="{StaticResource StrokeColor}"/>
        <SolidColorBrush x:Key="T1" Color="#F0FFFFFF"/>
        <SolidColorBrush x:Key="T2" Color="#70FFFFFF"/>
        <SolidColorBrush x:Key="T3" Color="#40FFFFFF"/>

        <!-- ═══ TOGGLE SWITCH ═══ -->
        <Style x:Key="Toggle" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Cursor="Hand" Width="42" Height="24">
                            <Border x:Name="Track" CornerRadius="12" Background="#20FFFFFF"/>
                            <Border x:Name="Thumb" Width="18" Height="18" CornerRadius="9"
                                    Background="#CCFFFFFF" HorizontalAlignment="Left" Margin="3,0,0,0"
                                    VerticalAlignment="Center">
                                <Border.RenderTransform>
                                    <TranslateTransform X="0"/>
                                </Border.RenderTransform>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Thumb"
                                                Storyboard.TargetProperty="(Border.RenderTransform).(TranslateTransform.X)"
                                                To="18" Duration="0:0:0.18">
                                                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <ColorAnimation Storyboard.TargetName="Track"
                                                Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                To="{StaticResource AccentColor}" Duration="0:0:0.18"/>
                                            <ColorAnimation Storyboard.TargetName="Thumb"
                                                Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                To="White" Duration="0:0:0.18"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Thumb"
                                                Storyboard.TargetProperty="(Border.RenderTransform).(TranslateTransform.X)"
                                                To="0" Duration="0:0:0.18">
                                                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <ColorAnimation Storyboard.TargetName="Track"
                                                Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                To="#20FFFFFF" Duration="0:0:0.18"/>
                                            <ColorAnimation Storyboard.TargetName="Thumb"
                                                Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                To="#CCFFFFFF" Duration="0:0:0.18"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ═══ DEVICE CARD ═══ -->
        <Style x:Key="DeviceCard" TargetType="Border">
            <Setter Property="Padding" Value="14,11"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="BorderBrush" Value="{StaticResource StrokeBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background">
                        <Setter.Value><SolidColorBrush Color="{StaticResource SurfaceHoverColor}"/></Setter.Value>
                    </Setter>
                </Trigger>
                <DataTrigger Binding="{Binding IsDefault}" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                    <Setter Property="Background">
                        <Setter.Value><SolidColorBrush Color="{StaticResource SurfaceSelectedColor}"/></Setter.Value>
                    </Setter>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- ═══ SHELL ═══ -->
    <Border CornerRadius="14" ClipToBounds="True" Background="#FF101014">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.35" Color="Black"/>
        </Border.Effect>
        <Grid>
            <!-- Gradient mesh -->
            <Border CornerRadius="14,14,0,0" VerticalAlignment="Top" Height="220"
                    IsHitTestVisible="False" Opacity="0.5">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#1A1565C0" Offset="0"/>
                        <GradientStop Color="#1A2196F3" Offset="0.3"/>
                        <GradientStop Color="#1000BCD4" Offset="0.6"/>
                        <GradientStop Color="#087C4DFF" Offset="0.85"/>
                        <GradientStop Color="#00000000" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
            </Border>

            <!-- Inner stroke -->
            <Border CornerRadius="14" BorderThickness="1" BorderBrush="#15FFFFFF" IsHitTestVisible="False"/>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="72"/>
                </Grid.RowDefinitions>

                <!-- ═══ TOP: Progress bar + Skip ═══ -->
                <Grid Grid.Row="0" Margin="0,0,0,0">
                    <!-- Progress bar track -->
                    <Border Height="3" VerticalAlignment="Top" Background="#08FFFFFF" CornerRadius="0,0,0,0">
                        <!-- Progress bar fill -->
                        <Border Background="{StaticResource AccentBrush}" CornerRadius="0,0,2,0"
                                HorizontalAlignment="Left" Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Border}}">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="ProgressScale" ScaleX="0.2"/>
                            </Border.RenderTransform>
                            <Border.RenderTransformOrigin>
                                <Point X="0" Y="0.5"/>
                            </Border.RenderTransformOrigin>
                        </Border>
                    </Border>

                    <!-- Title bar area for drag + Skip -->
                    <Grid Height="44" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                        <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center"
                                   Margin="0,0,20,0" Cursor="Hand"
                                   MouseLeftButtonDown="Skip_Click"
                                    FontSize="12" FontWeight="Medium" Text="{x:Static resx:Resources.OnboardingSkip}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#40FFFFFF"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#80FFFFFF"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Grid>

                <!-- ═══ CONTENT ═══ -->
                <Grid Grid.Row="1" Margin="48,0,48,0">

                    <!-- ══ PAGE 0: WELCOME ══ -->
                    <StackPanel x:Name="Page0" VerticalAlignment="Center"
                                Visibility="{Binding IsPage0, Converter={StaticResource BoolVis}}">
                        <StackPanel.RenderTransform>
                            <TranslateTransform x:Name="Page0Translate"/>
                        </StackPanel.RenderTransform>

                        <!-- Logo -->
                        <Grid HorizontalAlignment="Left" Margin="0,0,0,32">
                            <Border Width="64" Height="64" CornerRadius="16" Background="#15FFFFFF">
                                <Border.Effect>
                                    <DropShadowEffect Color="#2196F3" BlurRadius="24" ShadowDepth="0" Opacity="0.12"/>
                                </Border.Effect>
                                <Image Width="44" Height="44"
                                       Source="/BetterTrumpet;component/Assets/icon.png"
                                       RenderOptions.BitmapScalingMode="Fant"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>

                        <TextBlock Text="{x:Static resx:Resources.OnboardingWelcomeTo}" FontSize="14" FontWeight="Medium"
                                   Foreground="{StaticResource T2}" Margin="0,0,0,4"/>
                        <TextBlock Text="BetterTrumpet" FontSize="32" FontWeight="Bold"
                                   Foreground="{StaticResource T1}" Margin="0,0,0,14"/>
                        <TextBlock Text="{Binding SubtitleText}" FontSize="14" LineHeight="22"
                                   Foreground="{StaticResource T2}" TextWrapping="Wrap"
                                   MaxWidth="380" HorizontalAlignment="Left"/>
                        <TextBlock x:Name="VersionText" FontSize="11" Margin="0,20,0,0"
                                   Foreground="{StaticResource T3}"/>
                    </StackPanel>

                    <!-- ══ PAGE 1: AUDIO ══ -->
                    <StackPanel x:Name="Page1"
                                Visibility="{Binding IsPage1, Converter={StaticResource BoolVis}}">
                        <StackPanel.RenderTransform>
                            <TranslateTransform x:Name="Page1Translate"/>
                        </StackPanel.RenderTransform>

                        <TextBlock Text="&#xE767;" FontFamily="Segoe MDL2 Assets" FontSize="24"
                                   Foreground="{StaticResource AccentBrush}" Margin="0,8,0,16"/>
                        <TextBlock Text="{x:Static resx:Resources.OnboardingAudioOutput}" FontSize="24" FontWeight="SemiBold"
                                   Foreground="{StaticResource T1}" Margin="0,0,0,6"/>
                        <TextBlock Text="{Binding SubtitleText}" FontSize="13"
                                   Foreground="{StaticResource T2}" Margin="0,0,0,24"/>

                        <!-- Empty state when no devices -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16"
                                    Visibility="{Binding HasNoDevices, Converter={StaticResource BoolVis}}">
                            <TextBlock Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets" FontSize="16"
                                       Foreground="#FFD54F" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="{x:Static resx:Resources.OnboardingNoDevices}" FontSize="13"
                                       Foreground="{StaticResource T2}" VerticalAlignment="Center"/>
                        </StackPanel>

                        <ScrollViewer MaxHeight="310" VerticalScrollBarVisibility="Auto" Margin="0,0,-4,0">
                            <ItemsControl ItemsSource="{Binding AudioDevices}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource DeviceCard}"
                                                MouseLeftButtonDown="DeviceCard_Click" Tag="{Binding}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <!-- Name -->
                                                <TextBlock Grid.Column="0" Text="{Binding DisplayName}"
                                                           FontSize="13" VerticalAlignment="Center"
                                                           Foreground="{StaticResource T1}"
                                                           TextTrimming="CharacterEllipsis"/>
                                                <!-- Checkmark (visible when selected) -->
                                                <TextBlock Grid.Column="1" Text="&#xE73E;" FontFamily="Segoe MDL2 Assets"
                                                           FontSize="14" VerticalAlignment="Center"
                                                           Margin="10,0,0,0">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="Transparent"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsDefault}" Value="True">
                                                                    <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </StackPanel>

                    <!-- ══ PAGE 2: APPEARANCE (with mini previews) ══ -->
                    <StackPanel x:Name="Page2"
                                Visibility="{Binding IsPage2, Converter={StaticResource BoolVis}}">
                        <StackPanel.RenderTransform>
                            <TranslateTransform x:Name="Page2Translate"/>
                        </StackPanel.RenderTransform>

                        <TextBlock Text="&#xE790;" FontFamily="Segoe MDL2 Assets" FontSize="24"
                                   Foreground="{StaticResource AccentBrush}" Margin="0,8,0,16"/>
                        <TextBlock Text="{x:Static resx:Resources.OnboardingAppearance}" FontSize="24" FontWeight="SemiBold"
                                   Foreground="{StaticResource T1}" Margin="0,0,0,6"/>
                        <TextBlock Text="{Binding SubtitleText}" FontSize="13"
                                   Foreground="{StaticResource T2}" Margin="0,0,0,28"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- System (with mini preview) -->
                            <Border Grid.Column="0" CornerRadius="12" Cursor="Hand"
                                    MouseLeftButtonDown="Theme0_Click" ClipToBounds="True">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
                                        <Setter Property="BorderThickness" Value="1.5"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource StrokeBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedThemeIndex}" Value="0">
                                                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                                <Setter Property="Background">
                                                    <Setter.Value><SolidColorBrush Color="{StaticResource SurfaceSelectedColor}"/></Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background">
                                                    <Setter.Value><SolidColorBrush Color="{StaticResource SurfaceHoverColor}"/></Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <!-- Mini preview: dark mixer bars -->
                                    <Border Height="80" Background="#0AFFFFFF" CornerRadius="10,10,0,0" Padding="16,12">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom"
                                                    HorizontalAlignment="Center">
                                            <Rectangle Width="16" Height="36" RadiusX="3" RadiusY="3"
                                                       Fill="#30FFFFFF" Margin="0,0,6,0"/>
                                            <Rectangle Width="16" Height="52" RadiusX="3" RadiusY="3"
                                                       Fill="#25FFFFFF" Margin="0,0,6,0"/>
                                            <Rectangle Width="16" Height="28" RadiusX="3" RadiusY="3"
                                                       Fill="#20FFFFFF" Margin="0,0,6,0"/>
                                            <Rectangle Width="16" Height="44" RadiusX="3" RadiusY="3"
                                                       Fill="#30FFFFFF"/>
                                        </StackPanel>
                                    </Border>
                                    <StackPanel Margin="14,12,14,14">
                                        <TextBlock Text="{x:Static resx:Resources.OnboardingSystemTheme}" FontSize="13" FontWeight="SemiBold"
                                                   Foreground="{StaticResource T1}"/>
                                        <TextBlock Text="{x:Static resx:Resources.OnboardingSystemThemeDesc}" FontSize="11" Margin="0,2,0,0"
                                                   Foreground="{StaticResource T3}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- Custom (with colored preview) -->
                            <Border Grid.Column="2" CornerRadius="12" Cursor="Hand"
                                    MouseLeftButtonDown="Theme1_Click" ClipToBounds="True">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
                                        <Setter Property="BorderThickness" Value="1.5"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource StrokeBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedThemeIndex}" Value="1">
                                                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                                <Setter Property="Background">
                                                    <Setter.Value><SolidColorBrush Color="{StaticResource SurfaceSelectedColor}"/></Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background">
                                                    <Setter.Value><SolidColorBrush Color="{StaticResource SurfaceHoverColor}"/></Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <!-- Mini preview: colored mixer bars -->
                                    <Border Height="80" CornerRadius="10,10,0,0" Padding="16,12">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                <GradientStop Color="#101565C0" Offset="0"/>
                                                <GradientStop Color="#102196F3" Offset="0.5"/>
                                                <GradientStop Color="#107C4DFF" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom"
                                                    HorizontalAlignment="Center">
                                            <Rectangle Width="16" Height="36" RadiusX="3" RadiusY="3"
                                                       Fill="#503B9EFF" Margin="0,0,6,0"/>
                                            <Rectangle Width="16" Height="52" RadiusX="3" RadiusY="3"
                                                       Fill="#607C4DFF" Margin="0,0,6,0"/>
                                            <Rectangle Width="16" Height="28" RadiusX="3" RadiusY="3"
                                                       Fill="#5000BCD4" Margin="0,0,6,0"/>
                                            <Rectangle Width="16" Height="44" RadiusX="3" RadiusY="3"
                                                       Fill="#503B9EFF"/>
                                        </StackPanel>
                                    </Border>
                                    <StackPanel Margin="14,12,14,14">
                                        <TextBlock Text="{x:Static resx:Resources.OnboardingCustomTheme}" FontSize="13" FontWeight="SemiBold"
                                                   Foreground="{StaticResource T1}"/>
                                        <TextBlock Text="{x:Static resx:Resources.OnboardingCustomThemeDesc}" FontSize="11" Margin="0,2,0,0"
                                                   Foreground="{StaticResource T3}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>

                    <!-- ══ PAGE 3: PRIVACY (no dark pattern) ══ -->
                    <StackPanel x:Name="Page3"
                                Visibility="{Binding IsPage3, Converter={StaticResource BoolVis}}">
                        <StackPanel.RenderTransform>
                            <TranslateTransform x:Name="Page3Translate"/>
                        </StackPanel.RenderTransform>

                        <TextBlock Text="&#xE72E;" FontFamily="Segoe MDL2 Assets" FontSize="24"
                                   Foreground="{StaticResource AccentBrush}" Margin="0,8,0,16"/>
                        <TextBlock Text="{x:Static resx:Resources.OnboardingPrivacy}" FontSize="24" FontWeight="SemiBold"
                                   Foreground="{StaticResource T1}" Margin="0,0,0,6"/>
                        <TextBlock Text="{Binding SubtitleText}" FontSize="13"
                                   Foreground="{StaticResource T2}" Margin="0,0,0,28"/>

                        <!-- Telemetry -->
                        <Border Padding="16,14" CornerRadius="10" Margin="0,0,0,4"
                                Background="{StaticResource SurfaceBrush}"
                                BorderBrush="{StaticResource StrokeBrush}" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0">
                                    <TextBlock Text="{x:Static resx:Resources.OnboardingCrashReports}" FontSize="13" FontWeight="SemiBold"
                                               Foreground="{StaticResource T1}" Margin="0,0,0,3"/>
                                    <TextBlock Text="{x:Static resx:Resources.OnboardingCrashReportsDesc}" FontSize="12"
                                               Foreground="{StaticResource T3}"/>
                                </StackPanel>
                                <CheckBox Grid.Column="1" Style="{StaticResource Toggle}"
                                          IsChecked="{Binding IsTelemetryEnabled, Mode=TwoWay}"
                                          VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <!-- Reassuring line under telemetry (always visible) -->
                        <TextBlock Text="{x:Static resx:Resources.OnboardingCrashReportsInfo}"
                                   FontSize="11" Foreground="{StaticResource T3}" Margin="4,6,0,0"
                                   TextWrapping="Wrap"/>

                        <!-- Extra reassurance when user disables telemetry -->
                        <Border Margin="0,8,0,12" Padding="12,10" CornerRadius="8"
                                Background="#15FFD54F"
                                Visibility="{Binding ShowTelemetryReassurance, Converter={StaticResource BoolVis}}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets" FontSize="13"
                                           Foreground="#FFD54F" VerticalAlignment="Top" Margin="0,1,10,0"/>
                                <TextBlock FontSize="11.5" Foreground="{StaticResource T2}" TextWrapping="Wrap"
                                           MaxWidth="360"
                                           Text="{x:Static resx:Resources.OnboardingTelemetryReassurance}"/>
                            </StackPanel>
                        </Border>

                        <!-- Update channel -->
                        <Border Padding="16,14" CornerRadius="10" Margin="0,0,0,4"
                                Background="{StaticResource SurfaceBrush}"
                                BorderBrush="{StaticResource StrokeBrush}" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="{x:Static resx:Resources.OnboardingAutoUpdates}" FontSize="13" FontWeight="SemiBold"
                                           Foreground="{StaticResource T1}" Margin="0,0,0,10"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <!-- All -->
                                    <Border Grid.Column="0" Padding="0,7" CornerRadius="6" Cursor="Hand"
                                            Margin="0,0,3,0"
                                            MouseLeftButtonDown="OnboardingUpdateChannel0_Click">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#08FFFFFF"/>
                                                <Setter Property="BorderThickness" Value="1.5"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding UpdateChannelIndex}" Value="0">
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                                        <Setter Property="Background">
                                                            <Setter.Value><SolidColorBrush Color="{StaticResource AccentColor}" Opacity="0.1"/></Setter.Value>
                                                        </Setter>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock Text="{x:Static resx:Resources.OnboardingUpdateAll}" FontSize="12" FontWeight="SemiBold"
                                                       HorizontalAlignment="Center" Foreground="{StaticResource T1}"/>
                                            <TextBlock Text="{x:Static resx:Resources.OnboardingUpdateRecommended}" FontSize="9" Opacity="0.5"
                                                       HorizontalAlignment="Center" Foreground="{StaticResource AccentBrush}"/>
                                        </StackPanel>
                                    </Border>
                                    <!-- Minor+Major -->
                                    <Border Grid.Column="1" Padding="0,7" CornerRadius="6" Cursor="Hand"
                                            Margin="0,0,3,0"
                                            MouseLeftButtonDown="OnboardingUpdateChannel1_Click">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#08FFFFFF"/>
                                                <Setter Property="BorderThickness" Value="1.5"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding UpdateChannelIndex}" Value="1">
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                                        <Setter Property="Background">
                                                            <Setter.Value><SolidColorBrush Color="{StaticResource AccentColor}" Opacity="0.1"/></Setter.Value>
                                                        </Setter>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{x:Static resx:Resources.OnboardingUpdateMinor}" FontSize="12" FontWeight="SemiBold"
                                                   HorizontalAlignment="Center" Foreground="{StaticResource T1}"/>
                                    </Border>
                                    <!-- None -->
                                    <Border Grid.Column="2" Padding="0,7" CornerRadius="6" Cursor="Hand"
                                            MouseLeftButtonDown="OnboardingUpdateChannel3_Click">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#08FFFFFF"/>
                                                <Setter Property="BorderThickness" Value="1.5"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding UpdateChannelIndex}" Value="3">
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                                        <Setter Property="Background">
                                                            <Setter.Value><SolidColorBrush Color="{StaticResource AccentColor}" Opacity="0.1"/></Setter.Value>
                                                        </Setter>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{x:Static resx:Resources.OnboardingUpdateNone}" FontSize="12" FontWeight="SemiBold"
                                                   HorizontalAlignment="Center" Foreground="{StaticResource T1}"/>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- Startup -->
                        <Border Padding="16,14" CornerRadius="10"
                                Background="{StaticResource SurfaceBrush}"
                                BorderBrush="{StaticResource StrokeBrush}" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0">
                                    <TextBlock Text="{x:Static resx:Resources.OnboardingLaunchAtStartup}" FontSize="13" FontWeight="SemiBold"
                                               Foreground="{StaticResource T1}" Margin="0,0,0,3"/>
                                    <TextBlock Text="{x:Static resx:Resources.OnboardingLaunchAtStartupDesc}" FontSize="12"
                                               Foreground="{StaticResource T3}"/>
                                </StackPanel>
                                <CheckBox Grid.Column="1" Style="{StaticResource Toggle}"
                                          IsChecked="{Binding RunAtStartup, Mode=TwoWay}"
                                          VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <!-- ══ PAGE 4: READY (animated checkmarks) ══ -->
                    <StackPanel x:Name="Page4" VerticalAlignment="Center"
                                Visibility="{Binding IsPage4, Converter={StaticResource BoolVis}}">
                        <StackPanel.RenderTransform>
                            <TranslateTransform x:Name="Page4Translate"/>
                        </StackPanel.RenderTransform>

                        <!-- Check circle -->
                        <Border Width="52" Height="52" CornerRadius="26" HorizontalAlignment="Left"
                                Margin="0,0,0,28">
                            <Border.Background>
                                <SolidColorBrush Color="{StaticResource AccentColor}" Opacity="0.15"/>
                            </Border.Background>
                            <TextBlock Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" FontSize="22"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Foreground="{StaticResource AccentBrush}"/>
                        </Border>

                        <TextBlock Text="{x:Static resx:Resources.OnboardingAllReady}" FontSize="28" FontWeight="Bold"
                                   Foreground="{StaticResource T1}" Margin="0,0,0,8"/>
                        <TextBlock FontSize="13" TextWrapping="Wrap" LineHeight="20" MaxWidth="380"
                                   HorizontalAlignment="Left"
                                   Foreground="{StaticResource T2}" Margin="0,0,0,32"
                                   Text="{x:Static resx:Resources.OnboardingAllReadyDesc}"/>

                        <!-- Animated check items -->
                        <StackPanel x:Name="CheckItem0" Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                       Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"
                                       Margin="0,0,12,0"/>
                            <TextBlock Text="{x:Static resx:Resources.OnboardingDeviceConfigured}" FontSize="13"
                                       Foreground="{StaticResource T2}"/>
                        </StackPanel>
                        <StackPanel x:Name="CheckItem1" Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                       Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"
                                       Margin="0,0,12,0"/>
                            <TextBlock Text="{x:Static resx:Resources.OnboardingAppearanceChosen}" FontSize="13"
                                       Foreground="{StaticResource T2}"/>
                        </StackPanel>
                        <StackPanel x:Name="CheckItem2" Orientation="Horizontal" Margin="0,0,0,0">
                            <TextBlock Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                       Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"
                                       Margin="0,0,12,0"/>
                            <TextBlock Text="{x:Static resx:Resources.OnboardingPrivacyConfigured}" FontSize="13"
                                       Foreground="{StaticResource T2}"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- ══ PAGE 5: TRAY PIN (peak-end — must be the most premium page) ══ -->
                    <StackPanel x:Name="Page5" VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                Visibility="{Binding IsPage5, Converter={StaticResource BoolVis}}">
                        <StackPanel.RenderTransform>
                            <TranslateTransform x:Name="Page5Translate"/>
                        </StackPanel.RenderTransform>

                        <!-- Title centered -->
                        <TextBlock Text="{x:Static resx:Resources.OnboardingPinTitle}"
                                   FontSize="22" FontWeight="SemiBold"
                                   Foreground="{StaticResource T1}" HorizontalAlignment="Center"
                                   Margin="0,0,0,8"/>
                        <!-- Subtitle centered -->
                        <TextBlock FontSize="13" TextWrapping="Wrap" LineHeight="20"
                                   Foreground="{StaticResource T2}" TextAlignment="Center"
                                   HorizontalAlignment="Center" MaxWidth="380"
                                   Margin="0,0,0,20"
                                   Text="{x:Static resx:Resources.OnboardingPinDesc}"/>

                        <!-- GIF demo — large, centered, main visual -->
                        <Border CornerRadius="12" ClipToBounds="True" HorizontalAlignment="Center"
                                MaxWidth="420" MaxHeight="280"
                                Background="{StaticResource SurfaceBrush}"
                                BorderBrush="{StaticResource StrokeBrush}" BorderThickness="1"
                                Margin="0,0,0,16">
                            <Image x:Name="TrayPinGif" Stretch="Uniform"
                                   RenderOptions.BitmapScalingMode="Fant"/>
                        </Border>

                        <!-- Animated bouncing arrow ↓ — centered below GIF -->
                        <TextBlock x:Name="BouncingArrow" Text="&#xE74B;" FontFamily="Segoe MDL2 Assets"
                                   FontSize="20" Foreground="{StaticResource AccentBrush}"
                                   HorizontalAlignment="Center"
                                   RenderTransformOrigin="0.5,0.5">
                            <TextBlock.RenderTransform>
                                <TranslateTransform x:Name="ArrowBounce" Y="0"/>
                            </TextBlock.RenderTransform>
                        </TextBlock>
                    </StackPanel>
                </Grid>

                <!-- ═══ BOTTOM BAR ═══ -->
                <Grid Grid.Row="2" Margin="48,0,48,0" VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Back -->
                    <Border Grid.Column="0" Padding="14,8" CornerRadius="8" Cursor="Hand"
                            MouseLeftButtonDown="Back_Click"
                            Visibility="{Binding CanGoBack, Converter={StaticResource BoolVis}}">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#08FFFFFF"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock Text="{x:Static resx:Resources.OnboardingBack}" FontSize="13" FontWeight="Medium"
                                   Foreground="{StaticResource T3}"/>
                    </Border>

                    <!-- Step indicator dots -->
                    <StackPanel x:Name="StepDots" Grid.Column="1" Orientation="Horizontal"
                                HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Ellipse x:Name="Dot0" Width="6" Height="6" Margin="3,0">
                            <Ellipse.Fill><SolidColorBrush Color="{StaticResource AccentColor}"/></Ellipse.Fill>
                        </Ellipse>
                        <Ellipse x:Name="Dot1" Width="6" Height="6" Margin="3,0">
                            <Ellipse.Fill><SolidColorBrush Color="#25FFFFFF"/></Ellipse.Fill>
                        </Ellipse>
                        <Ellipse x:Name="Dot2" Width="6" Height="6" Margin="3,0">
                            <Ellipse.Fill><SolidColorBrush Color="#25FFFFFF"/></Ellipse.Fill>
                        </Ellipse>
                        <Ellipse x:Name="Dot3" Width="6" Height="6" Margin="3,0">
                            <Ellipse.Fill><SolidColorBrush Color="#25FFFFFF"/></Ellipse.Fill>
                        </Ellipse>
                        <Ellipse x:Name="Dot4" Width="6" Height="6" Margin="3,0">
                            <Ellipse.Fill><SolidColorBrush Color="#25FFFFFF"/></Ellipse.Fill>
                        </Ellipse>
                        <Ellipse x:Name="Dot5" Width="6" Height="6" Margin="3,0">
                            <Ellipse.Fill><SolidColorBrush Color="#25FFFFFF"/></Ellipse.Fill>
                        </Ellipse>
                    </StackPanel>

                    <!-- CTA button -->
                    <Border x:Name="CtaButton" Grid.Column="2" CornerRadius="20" Cursor="Hand"
                            MouseLeftButtonDown="Next_Click" RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform>
                            <ScaleTransform x:Name="CtaButtonScale" ScaleX="1" ScaleY="1"/>
                        </Border.RenderTransform>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Padding" Value="24,10"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background">
                                            <Setter.Value><SolidColorBrush Color="{StaticResource AccentHoverColor}"/></Setter.Value>
                                        </Setter>
                                    </Trigger>
                                    <!-- Wider on last page -->
                                    <DataTrigger Binding="{Binding IsLastPage}" Value="True">
                                        <Setter Property="Padding" Value="40,11"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock Text="{Binding NextButtonText}" FontSize="13" FontWeight="SemiBold"
                                   Foreground="#FF0E0E12"/>
                    </Border>
                </Grid>
            </Grid>

            <!-- ═══ CONFETTI CANVAS (overlay, no hit-test) ═══ -->
            <Canvas x:Name="ConfettiCanvas" IsHitTestVisible="False" ClipToBounds="True"/>
        </Grid>
    </Border>
</Window>

<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <SolidColorBrush x:Key="OnboardingBg" Color="#141418"/>
    <SolidColorBrush x:Key="OnboardingSurface" Color="#1B1B21"/>
    <SolidColorBrush x:Key="OnboardingSurfaceElevated" Color="#202028"/>
    <SolidColorBrush x:Key="OnboardingBorder" Color="#2B2B33"/>
    <SolidColorBrush x:Key="OnboardingBorderSoft" Color="#24242B"/>
    <SolidColorBrush x:Key="OnboardingAccent" Color="#3B9EFF"/>
    <SolidColorBrush x:Key="OnboardingAccentHover" Color="#57ACFF"/>
    <SolidColorBrush x:Key="OnboardingText" Color="#F3F5F8"/>
    <SolidColorBrush x:Key="OnboardingTextMuted" Color="#A8AAB3"/>
    <SolidColorBrush x:Key="OnboardingTextSubtle" Color="#7C7F88"/>
    <SolidColorBrush x:Key="OnboardingTrack" Color="#2A2D36"/>
    <SolidColorBrush x:Key="OnboardingTrackFill" Color="#3B9EFF"/>

    <Style x:Key="OnboardingPrimaryButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource OnboardingAccent}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="22,10"/>
        <Setter Property="MinWidth" Value="126"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource OnboardingAccentHover}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="OnboardingGhostButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource OnboardingTextMuted}"/>
        <Setter Property="Padding" Value="18,10"/>
        <Setter Property="MinWidth" Value="102"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="#0FFFFFFF"/>
                            <Setter Property="Foreground" Value="{StaticResource OnboardingText}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="OnboardingDangerButton" TargetType="Button" BasedOn="{StaticResource OnboardingGhostButton}">
        <Setter Property="Foreground" Value="#FCA5A5"/>
        <Setter Property="MinWidth" Value="126"/>
    </Style>

    <Style x:Key="OnboardingCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource OnboardingSurface}"/>
        <Setter Property="BorderBrush" Value="{StaticResource OnboardingBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="18"/>
    </Style>

    <Style x:Key="OnboardingChoiceCard" TargetType="Border" BasedOn="{StaticResource OnboardingCard}">
        <Setter Property="Cursor" Value="Hand"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource OnboardingSurfaceElevated}"/>
                <Setter Property="BorderBrush" Value="{StaticResource OnboardingAccent}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="OnboardingCardButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource OnboardingSurface}"/>
        <Setter Property="BorderBrush" Value="{StaticResource OnboardingBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="18"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource OnboardingSurfaceElevated}"/>
                            <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource OnboardingAccent}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="OnboardingCheckBox" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource OnboardingText}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Border Background="Transparent">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="Box"
                                    Width="18"
                                    Height="18"
                                    CornerRadius="4"
                                    Background="{StaticResource OnboardingSurfaceElevated}"
                                    BorderBrush="{StaticResource OnboardingBorder}"
                                    BorderThickness="1">
                                <TextBlock x:Name="Mark"
                                           Text="✓"
                                           Foreground="White"
                                           FontSize="12"
                                           FontWeight="SemiBold"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Opacity="0"/>
                            </Border>
                            <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Box" Property="Background" Value="{StaticResource OnboardingAccent}"/>
                            <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource OnboardingAccent}"/>
                            <Setter TargetName="Mark" Property="Opacity" Value="1"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
